package M_bookShop.stage_1_WeakConnection;

/**
 * 1. Предположим, у нас есть интерфейс Printable, который представляет сущность, способную выводить информацию:
 */
public interface Printable {
    void print();
}

